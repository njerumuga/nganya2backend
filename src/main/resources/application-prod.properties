# ===============================
# PRODUCTION + LOCAL SAFE CONFIG
# ===============================

# Database (SAFE FALLBACK)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d5nkgeje5dus73f78na0-a.oregon-postgres.render.com:5432/nganya_db}
spring.datasource.username=${DB_USER:nganya_db_user}
spring.datasource.password=${DB_PASS:sTo9eAU7i1jCO1NIwWV1QZ11TNvPQ99Y}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Timezone
spring.jackson.time-zone=Africa/Nairobi

# Server
server.port=${PORT:8080}

# ===============================
# FILE UPLOAD LIMITS (CLOUDINARY)
# ===============================
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# ===============================
# AUTH (JWT)
# ===============================
# Use a long random string in production (Render env: JWT_SECRET)
app.jwt.secret=${JWT_SECRET:dev-secret-change-me-dev-secret-change-me}
app.jwt.expiration-minutes=${JWT_EXP_MINUTES:43200}

